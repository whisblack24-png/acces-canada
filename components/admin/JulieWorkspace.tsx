"use client";
import { useEffect,useState } from "react";
import { Send,ShieldCheck,Sparkles } from "lucide-react";
type Entry={role:"director"|"julie";content:string};
export function JulieWorkspace(){const[entries,setEntries]=useState<Entry[]>([]),[message,setMessage]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState("");useEffect(()=>{fetch("/api/admin/julie").then(r=>r.json()).then((data:{messages?:Array<{role:string;content:string}>})=>setEntries(data.messages?.map(m=>({role:m.role==="julie"?"julie":"director",content:m.content}))||[])).catch(()=>setError("Historique indisponible."));},[]);async function send(){const value=message.trim();if(!value||busy)return;setEntries(v=>[...v,{role:"director",content:value}]);setMessage("");setBusy(true);setError("");const r=await fetch("/api/admin/julie",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:value})}),data=await r.json() as {answer?:string;error?:string};if(r.ok)setEntries(v=>[...v,{role:"julie",content:data.answer||"Demande reçue."}]);else setError(data.error||"Julie est indisponible.");setBusy(false)}return <div className="grid gap-6 xl:grid-cols-[1fr_320px]"><section className="overflow-hidden rounded-[2rem] bg-white shadow-premium"><div className="border-b border-navy/10 bg-navy p-6 text-white"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-gold text-navy"><Sparkles/></span><div><h2 className="font-display text-2xl font-black">Julie Mboyo</h2><p className="text-sm text-white/55">Assistante administrative du directeur</p></div></div></div><div className="h-[52vh] space-y-4 overflow-y-auto bg-ivory/60 p-5">{entries.length?entries.map((entry,index)=><div key={index} className={`max-w-[85%] rounded-2xl p-4 text-sm leading-6 ${entry.role==="julie"?"bg-white text-navy shadow-sm":"ml-auto bg-navy text-white"}`}><strong className="mb-1 block text-xs uppercase tracking-wider text-gold">{entry.role==="julie"?"Julie":"Christian"}</strong><div className="whitespace-pre-wrap">{entry.content}</div></div>):<p className="rounded-2xl bg-white p-4 text-sm text-navy/55">Bonjour Christian. Je suis prête à vous aider. Cette conversation sera conservée.</p>}{busy&&<p className="text-sm font-bold text-navy/45">Julie prépare sa réponse…</p>}</div><div className="border-t border-navy/10 p-4"><textarea value={message} onChange={e=>setMessage(e.target.value)} rows={3} placeholder="Julie, montre-moi les dossiers incomplets…" className="w-full rounded-2xl border border-navy/15 p-4 outline-none focus:ring-2 focus:ring-gold"/><div className="mt-3 flex items-center justify-between gap-3">{error?<p className="text-sm font-bold text-red-700">{error}</p>:<span/>}<button onClick={send} disabled={busy||!message.trim()} className="inline-flex items-center gap-2 rounded-xl bg-gold px-5 py-3 font-black text-navy disabled:opacity-40"><Send className="h-4 w-4"/>Envoyer</button></div></div></section><aside className="rounded-[2rem] bg-navy p-6 text-white shadow-premium"><ShieldCheck className="h-9 w-9 text-gold"/><h3 className="mt-4 font-display text-xl font-black">Contrôle du directeur</h3><p className="mt-3 text-sm leading-6 text-white/65">Les actions officielles, juridiques ou financières restent bloquées jusqu’à votre approbation.</p><p className="mt-4 text-xs leading-5 text-white/40">Julie fournit une assistance administrative et ne rend aucune décision juridique.</p></aside></div>}
