import type { TimelineEvent } from "@/lib/crm";
export function ClientTimeline({ events }: { events:TimelineEvent[] }) { return <ol className="relative ml-2 border-l-2 border-gold/40 pl-6">{events.length?events.map((event)=><li key={event.id} className="relative mb-5"><span className="absolute -left-[1.95rem] top-1 h-3 w-3 rounded-full bg-gold ring-4 ring-white"/><p className="text-xs font-black uppercase tracking-wider text-canada">{new Date(event.created_at).toLocaleString("fr-CA")}</p><strong className="mt-1 block">{event.title}</strong>{event.description?<p className="mt-1 text-sm text-navy/60">{event.description}</p>:null}</li>):<li className="text-sm text-navy/50">Aucun événement.</li>}</ol>; }
