import assert from "node:assert/strict";import test from "node:test";import { buildFinancialDashboard } from "../lib/finance.ts";import { formatUsd } from "../lib/format.ts";
test("la devise USD est uniforme",()=>{assert.equal(formatUsd(50),"50 USD");assert.equal(formatUsd(100),"100 USD");assert.equal(formatUsd(50.25),"50,25 USD")});
test("le tableau financier utilise les données de règlement Stripe",()=>{const appointment={id:"a",stripe_session_id:"cs_test_1",invoice_number:"F-1",client_full_name:"Client Test",confirmed_at:"2026-07-16T12:00:00Z",created_at:"2026-07-16T12:00:00Z",amount_cents:5000,stripe_settlement_currency:"CAD",stripe_settlement_gross_cents:6800,stripe_settlement_fee_cents:220,stripe_settlement_net_cents:6580,stripe_exchange_rate:1.36,stripe_refunded_cents:1000};const data=buildFinancialDashboard({appointments:[appointment] as never,payments:[],expenses:[],settings:{id:"default",usd_to_cad:1.35,stripe_fee_percent:2.9,stripe_fee_fixed_cents:30,updated_at:"",updated_by:""}});assert.equal(data.totals.grossUsd,50);assert.equal(data.totals.netCad,65.8);assert.equal(data.totals.refundedUsd,10);assert.equal(data.transactions[0].feeEstimated,false);assert.equal(data.transactions[0].test,true)});
