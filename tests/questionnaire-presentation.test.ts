import assert from "node:assert/strict"; import test from "node:test"; import { questionnaireInvitationEmail } from "../lib/questionnaire-email.ts"; import { generateQuestionnairePdf } from "../lib/questionnaire-pdf.ts"; import type { QuestionnaireRecord } from "../lib/questionnaires.ts";
const row: QuestionnaireRecord = { id: "q1", client_id: "c1", questionnaire_type: "client_principal", status: "draft", answers_encrypted: "", section_progress: {}, progress_percent: 12, respondent_name: null, respondent_email: null, created_at: "2026-07-01T12:00:00Z", updated_at: "2026-07-02T12:00:00Z", last_saved_at: null, submitted_at: null };
test("le courriel contient la marque, le bouton, l'expiration et la sécurité", () => { const email = questionnaireInvitationEmail({ name: "Jean Exemple", type: "client_principal", url: "https://example.com/q/secret", expiresAt: "2026-07-20T12:00:00Z", siteUrl: "https://acces-canada.vercel.app" }); assert.match(email.html, /Logo Accès Canada/); assert.match(email.html, /Remplir mon questionnaire/); assert.match(email.html, /Expiration du lien/); assert.match(email.html, /Ne le transférez pas/); assert.match(email.text, /Lien sécurisé/); });
test("le PDF est paginé, illustré et renseigne les champs vides", () => { const pdf = generateQuestionnairePdf(row, { family_name: "Exemple" }, { fullName: "Jean Exemple", fileReference: "AC-2026-0001" }); assert.equal(pdf.subarray(0, 8).toString("latin1"), "%PDF-1.4"); assert.ok(pdf.length > 15000); const source = pdf.toString("latin1"); assert.match(source, /\/Subtype \/Image/); assert.match(source, /Document confidentiel - Acces Canada/); assert.match(source, /Non renseigne/); assert.match(source, /Page 1 \/ /); });
