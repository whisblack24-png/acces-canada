import { redirect } from "next/navigation";import { AdminShell } from "@/components/admin/AdminShell";import { AppointmentAutomationSettings } from "@/components/admin/AppointmentAutomationSettings";import { isAdminAuthenticated } from "@/lib/admin-auth";import { getAppointmentAutomationSettings } from "@/lib/appointment-automation";
export default async function Page(){if(!(await isAdminAuthenticated()))redirect("/admin/login");return <AdminShell><div className="space-y-7"><header><p className="text-xs font-black uppercase tracking-[.24em] text-canada">Rendez-vous</p><h1 className="mt-2 font-display text-4xl font-black">Paiements et rappels automatiques</h1></header><AppointmentAutomationSettings initial={await getAppointmentAutomationSettings()}/></div></AdminShell>}
