import { NextResponse, type NextRequest } from "next/server"; import { getClientSession } from "@/lib/client-auth"; import { createQuestionnaireSession, getClientQuestionnaire, SESSION_COOKIE } from "@/lib/questionnaires";
export async function GET(request: NextRequest) { const session = await getClientSession(); if (!session) return NextResponse.redirect(new URL("/client/login", request.url)); const row = await getClientQuestionnaire(session.clientId, "client_principal"); if (!row) return NextResponse.redirect(new URL("/client/dashboard", request.url)); const expiresAt = new Date(Date.now() + 6 * 3600000).toISOString(); const response = NextResponse.redirect(new URL("/questionnaire", request.url)); response.cookies.set(SESSION_COOKIE, createQuestionnaireSession(row.id, expiresAt), { httpOnly: true, sameSite: "lax", secure: process.env.NODE_ENV === "production", path: "/", expires: new Date(expiresAt) }); return response; }

