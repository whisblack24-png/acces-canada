import { NextResponse } from "next/server"; import { isAdminAuthenticated } from "@/lib/admin-auth"; import { getClientQuestionnaire, saveQuestionnaire, type QuestionnaireAnswers } from "@/lib/questionnaires"; import type { QuestionnaireType } from "@/lib/questionnaire-definitions";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string; type: string }> }) { if (!(await isAdminAuthenticated())) return NextResponse.json({ error: "Non autorisé." }, { status: 401 }); const { id, type } = await params; const row = await getClientQuestionnaire(id, type as QuestionnaireType); if (!row) return NextResponse.json({ error: "Introuvable." }, { status: 404 }); try { const body = await request.json() as { answers: QuestionnaireAnswers; submit?: boolean }; const saved = await saveQuestionnaire(row, body.answers || {}, body.submit === true); return NextResponse.json(saved); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Erreur." }, { status: 400 }); } }

