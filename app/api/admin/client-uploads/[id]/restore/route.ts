import { NextResponse } from "next/server"; import { isAdminAuthenticated } from "@/lib/admin-auth"; import { restoreClientFileVersion } from "@/lib/client-portal"; import { createAuditLog } from "@/lib/platform-v2";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!(await isAdminAuthenticated()))return NextResponse.json({error:"Non autorisé."},{status:401});const{id}=await params;const clientId=new URL(request.url).searchParams.get("clientId");if(!clientId)return NextResponse.json({error:"Client requis."},{status:400});try{const restored=await restoreClientFileVersion(clientId,id);await createAuditLog({action:"restore",entityType:"client_uploaded_documents",entityId:id,clientId,summary:`Version ${restored.version} restaurée`});return NextResponse.json(restored)}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Restauration impossible."},{status:400})}}
