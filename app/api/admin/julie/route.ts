import { NextResponse } from "next/server";
import { getAdminIdentity,isAdminAuthenticated } from "@/lib/admin-auth";
import { askJulie,createJulieApproval } from "@/lib/julie";
export async function POST(request:Request){if(!(await isAdminAuthenticated()))return NextResponse.json({error:"Non autorisé."},{status:401});const body=await request.json() as {message?:string;clientId?:string};const message=String(body.message||"").trim().slice(0,4000);if(!message)return NextResponse.json({error:"Écrivez une demande à Julie."},{status:400});try{const identity=await getAdminIdentity(),result=await askJulie(message);let approval=null;if(result.sensitiveAction)approval=await createJulieApproval({clientId:body.clientId,staffId:identity?.id,actionType:result.sensitiveAction,title:`Demande préparée par Julie — ${result.sensitiveAction.replaceAll("_"," ")}`,description:message,payload:{source:"julie_conversation"}});return NextResponse.json({...result,approval});}catch(error){console.error("Julie",error);return NextResponse.json({error:"Julie n’a pas pu traiter cette demande. Aucune action n’a été exécutée."},{status:500});}}
